<div class="search_first">
  <%= render 'posts/search_form' %>
</div>
<div class="test">
  <%= link_to(image_tag("a.png"), controller: "searchs", action: "index", category_id: 5) %>
</div>

<section class="toppage">
  <div class="slide_part">
    <ul class="slide">
      <li class="newst-hoge"><%= image_tag "test4.jpg" %></li>
      <li class="nest-hoge"><%= image_tag "test5.jpg" %></li>
      <li class="nest-hoge"><%= image_tag "test6.jpg" %></li>
      <li class="nest-hoge"><%= image_tag "test8.jpg" %></li>
      <li class="nest-hoge"><%= image_tag "test9.jpg" %></li>
    </ul>
    <h4>自分らしい生活を見つけるための場所</h4>
    <p>今の社会情勢をきっかけに住む場所を決める優先順位は、勤務地の通いやすさから暮らしの充実度に変わりつつあります。<br>
    どこに住んで、どんなことをして、どんな毎日を送りたいのかを一緒に見つけていきましょう。</p>
  </div>
  <div class="photo_part">
    <p>最近の投稿</p>
    <% @posts.each do |post| %>
      <ul class="photo_post">
        <li class="top_photo">
          <% if post.image.present? %>
            <%= link_to image_tag(post.image.url), "/posts/#{post.id}" %>
          <% end %>
        </li>
        <ul class="photo_info">
          <li class="top_first_line">
            <%= link_to image_tag(post.user.image.url),  user_path(post.user) %>
            <%= link_to post.user.name, user_path(post.user) %> 
            <span class="created_at"><%= post.created_at.strftime('%Y/%m/%d')  %></span>
          </li>
          <li class="top_title">
            <%= post.title %>
          </li>
          <li class="top_content">
            <%= post.content %>
          </li>
          
        </ul>
      </ul>
    <% end %>
    <%= paginate @posts %>
  </div>
</section>

